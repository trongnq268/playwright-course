/**
 * Lesson 04 – hướng dẫn chạy bài tập (không phải code làm bài)
 *
 * Quy định nộp bài đã áp dụng:
 *  - 8 bài mỗi bài 1 file riêng: bai1.ts → bai8.ts
 *  - Import/export: types/ | data/ | config/
 *  - Không dùng Function / Class
 *  - Bài 1–8 chỉ dùng if / for...of / import-export / type
 *
 * Cách chạy bằng TypeScript:
 *   npx --yes tsx tests/lesson04/bai1.ts
 *   npx --yes tsx tests/lesson04/bai2.ts
 *   ...
 *   npx --yes tsx tests/lesson04/bai8.ts
 *   npx --yes tsx tests/lesson04/bonus-bai8.ts
 *
 * (File .spec này cố ý để trống test — nộp bài theo các file bai*.ts)
 */
import { test } from '@playwright/test';

test.skip('Xem hướng dẫn chạy bài trong comment đầu file — nộp bài bằng bai1.ts → bai8.ts', () => {});
